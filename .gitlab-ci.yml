image: docker:stable

# IIRC we use socket binding, if not, uncomment this:
#variables:
#  DOCKER_HOST: tcp://docker:2375/
#  DOCKER_DRIVER: overlay2
#
#services:
#  - docker:dind

before_script:
  - docker info

build:
  stage: build
  script:
    - docker build -t soseth/python-ci-base:$CI_COMMIT_REF_NAME .
    - docker push soseth/python-ci-base:$CI_COMMIT_REF_NAME
